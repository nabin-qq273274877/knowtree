# knowtree 需求文档 V1

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 状态 | 草案（待评审） |
| 产品名称 | knowtree（知识树） |
| 更新日期 | 2026-02-05 |

---

## 1. 项目概述

### 1.1 背景

个人使用的知识点管理系统，覆盖从幼儿园到大学全部学段的知识点。核心诉求是：把庞杂的知识组织成**树形结构**（层级归属），同时支持在任意知识点之间建立**自由关联**（网状联系），并跟踪每个知识点的**学习状态**，后续接入大语言模型辅助讲解与内容生产。

### 1.2 目标

- 单人使用的本地优先（local-first）应用，部署简单、数据自主可控
- 知识点的树形管理与自由关联图谱双视图
- 学习状态可视化追踪
- LLM 辅助：知识点讲解、按主题批量生成知识点子树
- 一个可执行文件（或一条 docker 命令）即可跑起来

### 1.3 非目标（本期不做）

- ❌ 多用户系统、注册登录、权限管理
- ❌ 云端多人协作 / 实时同步（未来可通过导出/网盘同步数据文件解决）
- ❌ 预置"全量知识点数据"——系统是**管理工具**而非数据库本身，数据来源见第 4 节
- ❌ 移动端原生 App（Web 页面保证基本可用即可）

### 1.4 使用者画像

| 角色 | 说明 |
| --- | --- |
| 唯一用户 | 即所有者本人。桌面浏览器为主要使用环境，无并发编辑场景 |

---

## 2. 功能需求

优先级定义：P0 = v1 必须；P1 = v1 尽量有；P2 = 后续版本。

### FR-1 知识树管理（P0）

树形视图展示知识点层级（如：学段 → 学科 → 章节 → 知识点 → 子知识点），深度不限。

每个节点支持：

| 操作 | 说明 | 优先级 |
| --- | --- | --- |
| 新增下级节点 | 在任意节点下添加子节点 | P0 |
| 新增同级节点 | 在当前节点后插入兄弟节点 | P0 |
| 编辑 | 修改标题（及备注） | P0 |
| 删除 | 删除节点；**含子树的级联删除必须二次确认并提示影响范围** | P0 |
| 拖拽移动 | 拖拽改变父节点（挂到其他节点下）与同层排序；拖拽过程要有明确的放置指示 | P0 |
| 展开/折叠 | 支持展开全部/折叠全部、记忆展开状态 | P1 |
| 搜索定位 | 按标题搜索并高亮定位到树中节点 | P0 |

约束：

- 幼儿园/小学/初中/高中/大学等**学段**建议作为顶层预设骨架（首次启动时可一键生成空骨架，也可完全自由组织）——见开放问题 Q4
- 节点数量预期可达数千，树组件需虚拟滚动/懒加载

### FR-2 关联图谱（画布视图）（P0）

除树形视图外，提供自由布局的**图视图**：

| 操作 | 说明 | 优先级 |
| --- | --- | --- |
| 节点呈现 | 每个知识点是一个卡片节点，显示标题与状态色 | P0 |
| 节点拖拽 | 自由拖动节点位置，坐标持久化 | P0 |
| 建立连线 | 任意两个节点之间可拉线连接 | P0 |
| 删除连线 | 点选连线后删除 | P0 |
| 连线拖拽 | 拖动连线端点，将其改接到其他节点（重连） | P1 |
| 连线类型 | 区分「前置依赖」（有方向，表示学习顺序）与「一般关联」（无方向） | P1 |
| 画布导航 | 缩放、平移、小地图（minimap）、适应屏幕 | P1 |

约定：

- **父子关系不算连线**：树的层级由 `parent_id` 表达；连线专指跨层级的自由关联，二者分开存储
- 图视图可按"当前树中选中的分支"过滤显示范围，避免数千节点全量铺开卡死
- 从连线点击节点可打开详情（与树视图一致）

### FR-3 节点详情（P0）

点击任意节点（树视图或图视图）打开详情面板：

- 标题、所属路径（面包屑）
- **正文内容：Markdown 编辑与渲染，必须支持数学公式（LaTeX → KaTeX 渲染）**——从 K12 到大学数学公式是刚需
- 学习状态切换（见 FR-4）
- 关联教学资源列表（见 FR-5）
- 关联知识点列表（通过连线关联的节点，可点击跳转）
- 上级/下级导航

### FR-4 学习状态体系（P0）

每个节点具有互斥的学习状态，共 5 种起步：

| 状态 | 含义 | 视觉 |
| --- | --- | --- |
| 未学 | 尚未开始 | 灰 |
| 学习中 | 正在学 | 蓝 |
| 部分学会 | 掌握了一部分 | 黄 |
| 已学会 | 已掌握 | 绿 |
| 已遗忘 | 曾经学会但已遗忘，需重学 | 红 |

- 树视图与图视图中均以颜色/图标直观区分
- 支持按状态筛选（如只看"未学"）、统计各状态占比
- 状态变更记录时间戳（为将来的遗忘曲线/复习提醒留数据基础）

### FR-5 教学资源关联（P0）

每个节点可挂载多条教学资源：

- 类型：**外部链接**（文档网页、视频页如 B 站/YouTube、在线课程等）为 P0；**本地上传文件**（PDF 等，存数据目录）为 P1
- 字段：标题、URL/文件、类型标签（视频/文档/习题/其他）、备注
- 视频类链接尽量支持站内嵌入式播放（如 B 站 iframe 嵌入），至少支持一键新开
- 列表可排序、可删除

### FR-6 LLM 集成（P0）

通过 **OpenAI 兼容 API** 接入大模型（天然兼容 DeepSeek、Kimi、OpenRouter、通义、本地 Ollama/vLLM 等）：

| 能力 | 说明 | 优先级 |
| --- | --- | --- |
| 讲解知识点 | 详情面板内发起，结合节点上下文（标题、正文、父级/前置节点）生成讲解；**流式输出**；支持追加提问（简易会话） | P0 |
| 生成知识点子树 | 输入主题（如"人教版八年级物理·浮力"），LLM 生成层级知识点 JSON，**预览确认后再入库**（不直接写库） | P1 |
| 用量记录 | 记录每次调用的 token 消耗，设置面板可见累计用量 | P2 |

安全与健壮性：API Key 仅存本地数据库，界面脱敏显示；请求失败有明确错误提示与重试。

### FR-7 设置面板（P0）

- **LLM 配置**：API Base URL、API Key、模型名、temperature、最大 tokens；提供「测试连接」按钮
- **数据管理**：数据目录位置展示、一键备份（下载/另存 SQLite 副本或 JSON）、从备份恢复
- **界面偏好**：暗色/亮色主题（P1）、树默认展开层数
- 所有配置即时生效，无需重启

### FR-8 数据来源方案（P0，回应"知识点数据从哪来"）

系统不预置数据，提供三条获取通道，可叠加使用：

1. **LLM 生成（主通道）**：按学段/学科/主题让 LLM 批量生成知识点树草稿 → 人工预览、删改 → 确认入库。适合快速铺开某个学科的骨架。
2. **手动编辑**：日常增删改，随学习进度逐步充实正文内容。
3. **导入**：
   - JSON（knowtree 自有格式，见导出）—— P0
   - Markdown 有序/无序列表大纲（缩进即层级，便于从笔记迁移）—— P1
   - 第三方现成知识库/课标数据（教育部课程标准整理稿、开源知识图谱等）：无统一格式，**不做通用适配器**，需要时写一次性转换脚本转成 JSON 再导入
4. **导出（数据自主权兜底）**：全量导出 JSON（P0）；按子树导出（P1）

> 结论：数据来源以「LLM 初稿 + 人工校对」为主线，JSON 导入导出保证不被锁定。

---

## 3. 技术栈决策

### 3.1 选型原则

1. 部署极简：最好打成**单个可执行文件**；做不到就用 **Docker 一条命令**
2. 前端重交互（树 + 图画布），选生态成熟的方案，不自研画布
3. 数据零运维：**SQLite** 单文件，备份=复制文件
4. 单人项目，控制语言数量，前后端尽量同一种语言、类型共享

### 3.2 推荐方案：TypeScript 全栈（Bun + React）

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 运行时 | **Bun** | 内置 `bun:sqlite`（无需编译原生模块）；`bun build --compile` 可打出**单文件可执行程序**（Windows/Linux/macOS），满足"类 Go 的二进制分发"诉求 |
| 后端框架 | **Hono** | 轻量、TS 原生、内置静态资源服务与 SSE 流式支持（LLM 流式输出要用） |
| ORM | **Drizzle ORM** | 类型安全、对 SQLite 友好、迁移轻量 |
| 前端框架 | **React 18 + Vite + TypeScript** | 生态最成熟，图画布库都长在这个生态上 |
| UI 组件 | Tailwind CSS + shadcn/ui | 快速搭出体面的管理界面，暗色主题免费获得 |
| 树组件 | **react-arborist**（备选：自研 + 虚拟列表） | 虚拟滚动的树，原生支持拖拽排序/换父 |
| 图画布 | **@xyflow/react（React Flow 12）** | 节点拖拽、连线增删、**连线端点重连**、缩放平移、minimap 全部内置，与 FR-2 几乎一一对应，是最关键的一个选型 |
| 状态/请求 | Zustand + TanStack Query | 轻量够用 |
| 公式渲染 | react-markdown + remark-math + KaTeX | Markdown 与 LaTeX 公式渲染 |

**部署形态（两条都要提供）：**

- 形态 A（默认推荐）：`bun build --compile` 打包**单个可执行文件**，前端构建产物一并打入；后端自己 serve 前端静态资源（Hono `serveStatic`），**全程不依赖 nginx**，只有一个端口
- 形态 B：提供 `Dockerfile` + `docker-compose.yml`（volume 挂载数据目录），供不便使用二进制的场景

### 3.3 备选方案对比

| 方案 | 二进制 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- | --- |
| **Bun + Hono + SQLite（推荐）** | ✅ 单文件 | 前后端同语言、类型共享、迭代最快、单文件分发 | Bun 相对新（锁定版本、注意兼容） | ✅ 采用 |
| Go（chi/Gin + modernc.org/sqlite + go:embed） | ✅ 最小（约 20MB） | 真·单二进制、内存占用低、交叉编译无脑 | 前后端两种语言、类型不共享、开发速度慢一截 | ⭐ 最强备选，若在意运行时资源可切换 |
| Node.js + Fastify | ❌（SEA 不成熟） | 生态稳 | 无法优雅打二进制 → 只剩 Docker 一条路 | 不采用 |
| Python + FastAPI | ❌（PyInstaller 臃肿易碎） | AI 生态好 | 分发差、性能一般、仍需 Docker | 不采用 |
| Tauri/Electron 桌面应用 | 安装包 | 天然本地应用 | 多一套桌面工程复杂度，浏览器访问没了 | 不采用（保持 Web 形态） |

> 决策：**采用 Bun + Hono + SQLite + React + React Flow**。若后期对 Bun 稳定性不满，仅后端层可平移至 Go，前端不受影响（API 契约不变）。

---

## 4. 系统架构草案

```
┌────────────────────────── 浏览器 ──────────────────────────┐
│  树视图(react-arborist)   图视图(@xyflow/react)   详情面板   │
└──────────────▲───────────────────────────▲────────────────┘
               │ 静态资源                   │ REST + SSE (/api/*)
┌──────────────┴───────────────────────────┴────────────────┐
│                单进程后端（Bun + Hono）                     │
│  serveStatic(前端产物) · REST API · LLM 代理(SSE 流式转发)  │
│                    Drizzle ORM                             │
└───────────────────────────┬───────────────────────────────┘
                            │
                    SQLite（单文件，数据目录内）
                    nodes / edges / resources / settings / ...
```

要点：

- **单进程、单端口**：后端既提供 API 也托管前端静态文件，无 nginx/无独立前端服务器
- LLM 调用由后端代理转发（API Key 不下发到浏览器），流式响应用 SSE
- 数据目录默认 `./data`（可用环境变量 `KNOWTREE_DATA_DIR` 覆盖），内含 `knowtree.db`

### API 草案（REST，前缀 `/api`）

```
GET/POST        /nodes              列表/新增
PATCH/DELETE    /nodes/:id          编辑/删除
POST            /nodes/:id/move     拖拽换父/排序
GET/POST        /edges              连线列表/新建
PATCH/DELETE    /edges/:id          重连端点/改类型/删除
GET/POST        /nodes/:id/resources  资源列表/新增
DELETE          /resources/:id
GET             /search?q=          标题搜索
GET/PUT         /settings           配置读写
POST            /llm/explain        讲解（SSE 流式）
POST            /llm/generate-subtree  生成子树草稿（返回 JSON，不直接入库）
POST            /import             导入 JSON
GET             /export             导出 JSON
GET             /stats              状态统计
```

---

## 5. 数据模型草案

```sql
-- 知识点（树用 parent_id 表达，无限层级）
CREATE TABLE nodes (
  id          TEXT PRIMARY KEY,
  title       TEXT NOT NULL,
  content_md  TEXT NOT NULL DEFAULT '',       -- Markdown 正文（含 LaTeX）
  status      TEXT NOT NULL DEFAULT 'not_started'
              CHECK (status IN ('not_started','learning','partial','mastered','forgotten')),
  stage       TEXT,                           -- 学段标签（幼儿园/小学/…，冗余便于筛选）
  sort_order  REAL NOT NULL DEFAULT 0,
  parent_id   TEXT REFERENCES nodes(id) ON DELETE CASCADE,
  pos_x REAL, pos_y REAL,                     -- 图视图坐标
  status_changed_at INTEGER,                  -- 状态变更时间（为复习提醒留底）
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 自由关联连线（不含父子关系）
CREATE TABLE edges (
  id         TEXT PRIMARY KEY,
  source_id  TEXT NOT NULL REFERENCES nodes(id) ON DELETE CASCADE,
  target_id  TEXT NOT NULL REFERENCES nodes(id) ON DELETE CASCADE,
  relation   TEXT NOT NULL DEFAULT 'related'
             CHECK (relation IN ('prerequisite','related')), -- 前置依赖/一般关联
  label      TEXT,
  created_at INTEGER NOT NULL,
  UNIQUE (source_id, target_id, relation)
);

-- 教学资源
CREATE TABLE resources (
  id      TEXT PRIMARY KEY,
  node_id TEXT NOT NULL REFERENCES nodes(id) ON DELETE CASCADE,
  kind    TEXT NOT NULL DEFAULT 'link',       -- link / file
  title   TEXT NOT NULL,
  url     TEXT,                               -- link 时必填
  path    TEXT,                               -- file 时相对数据目录
  note    TEXT,
  created_at INTEGER NOT NULL
);

-- 设置（KV）
CREATE TABLE settings ( key TEXT PRIMARY KEY, value_json TEXT NOT NULL );

-- FTS5 虚表：标题+正文全文搜索（P1）
```

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | ≥ 5000 节点时树/图交互流畅（树虚拟滚动；图按分支过滤渲染）；常规 API < 50ms |
| 部署 | 单可执行文件或 `docker compose up -d`；仅暴露一个端口（默认 `3000`，可配） |
| 前端托管 | 后端 `serveStatic` 自托管，**不依赖 nginx 等任何第三方 Web 服务器** |
| 监听地址 | 默认 `127.0.0.1`（单人本机）；允许配置为 `0.0.0.0` 局域网访问，此时应启用可选的访问口令（见 §7-S9） |
| 数据安全 | SQLite 单文件；提供一键备份/恢复；升级程序不改坏旧库（Drizzle 迁移向前兼容） |
| 浏览器兼容 | 最新版 Chrome/Edge/Firefox；不考虑 IE |
| 平台 | Windows / Linux（macOS 顺手支持） |

---

## 7. 补充：原需求未提及但必要的项

以下为评审时补齐的必要项（对应需求第 11 条）：

| # | 项 | 说明 | 优先级 |
| --- | --- | --- | --- |
| S1 | **数学公式渲染** | K12~大学内容离不开 LaTeX，Markdown 渲染链路必须带 KaTeX | P0 |
| S2 | **搜索** | 几千个节点没有搜索不可用；标题搜索 P0，全文搜索 P1 | P0/P1 |
| S3 | **撤销/重做** | 树/图的误删误拖很常见，编辑操作需支持 Ctrl+Z/Ctrl+Y（至少删除类操作） | P1 |
| S4 | **删除保护** | 级联删除二次确认 + 明示影响节点数；P1 可加回收站（软删除） | P0/P1 |
| S5 | **备份与恢复** | 单文件数据库也怕误删/磁盘故障，设置面板内置备份入口 | P0 |
| S6 | **导入导出** | 见 FR-8，数据自主权底线 | P0 |
| S7 | **空状态与引导** | 首次启动提供「生成学段骨架」「导入 JSON」「空白开始」三选项，避免打开一片空白不知所措 | P0 |
| S8 | **统计概览** | 各学段/学科的状态分布、总进度，一张简单的仪表页 | P1 |
| S9 | **最小访问防护** | 无用户系统 ≠ 完全裸奔：可选访问口令（局域网暴露场景） | P1 |
| S10 | **LLM Key 安全** | Key 只存本地、后端代理调用、界面脱敏 | P0 |
| S11 | **多标签页一致性** | 同一浏览器多标签同时打开时数据不互相踩（TanStack Query 失效策略/轻量轮询即可，不必上 WebSocket） | P1 |
| S12 | **快捷键** | Tab/Enter 快速建兄弟/子节点、Delete 删除、Ctrl+F 搜索——高频操作的效率底线 | P1 |

---

## 8. 开放问题（需拍板）

| # | 问题 | 建议 |
| --- | --- | --- |
| Q1 | 连线是否区分「前置依赖/一般关联」两类？ | 建议区分，成本低、对学习顺序价值大（FR-2 已按两类设计） |
| Q2 | 「已遗忘」要不要触发复习提醒（间隔重复/遗忘曲线）？ | v1 先记录状态时间戳不做提醒，v2 加复习队列 |
| Q3 | 知识点粒度怎么定（多细算一个节点）？ | 不做硬性规定，靠 LLM 生成时的 prompt 约定粒度 + 使用中自然形成习惯 |
| Q4 | 学段-学科是否作为固定顶层骨架？ | 建议：首次启动可一键生成「幼儿园→大学 × 主要学科」空骨架，之后完全自由 |
| Q5 | 视频资源站内嵌入播放还是仅跳转？ | B 站等支持 iframe 嵌入的做嵌入，其余跳转 |
| Q6 | 是否需要暗色主题？ | 建议要（shadcn 成本几乎为零），P1 |
| Q7 | 正文是否需要图片上传/粘贴？ | 建议 P1 支持（存数据目录，Markdown 引用），纯文字讲不清的知识点不少 |

---

## 9. 里程碑建议

| 里程碑 | 内容 | 验收标志 |
| --- | --- | --- |
| M1 骨架 | 工程搭建、SQLite+迁移、节点树 CRUD API + 树视图（增删改/拖拽/搜索） | 能手工录一棵树并持久化 |
| M2 图谱 | React Flow 图视图、连线增删/重连、坐标持久化、状态着色 | 双视图联动同一份数据 |
| M3 详情 | 详情面板、Markdown+KaTeX、资源链接管理、状态切换与筛选 | 节点内容完整可用 |
| M4 LLM | 设置面板、讲解（流式）、生成子树预览入库 | 三大 LLM 能力跑通 |
| M5 收尾 | 导入导出、备份恢复、统计页、撤销重做、单文件打包 + Docker 发布 | 一个文件/一条命令部署成功 |

---

*本文档为 v1 草案。开放问题（§8）确认后升为 v1.1 定稿。*
